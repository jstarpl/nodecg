import { createTheme } from "@mui/material/styles";

export const nodeCGTheme = createTheme({
  palette: {
    mode: "dark",
    primary: {
      main: "#00ADEE",
    },
    secondary: {
      main: "#004D6A",
    },
  },
});
